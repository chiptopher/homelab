networks:
  n8n_n8n_network:
    external: true

services:
  web-print:
    build: .
    restart: always
    ports:
      - "8631:8631"
    environment:
      - CUPS_SERVER=host.docker.internal
    networks:
      - n8n_n8n_network
